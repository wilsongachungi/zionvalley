name: Deploy Private GitHub repository to cPanel
on:
  push:
    branches:
      - main
jobs:
  FTP-Deploy-Action:
    name: FTP-Deploy-Action
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2.1.0
      with:
        fetch-depth: 2
    - name: FTP-Deploy-Action
      uses: SamKirkland/FTP-Deploy-Action@4.3.4
      with:
        server: ftp.zionvalleycbo.org
        username: gitsample@zionvalleycbo.org
        password: T3stFtp@123456
				exclude: |
          **/.git*
          **/.git*/**
          **/.vite*/**
          **/public/**
          **/storage/public/**
